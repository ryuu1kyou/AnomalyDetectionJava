package com.anomalydetection.shared.safety;

/** Approval lifecycle of a change request attached to a safety trace record. */
public enum ChangeApprovalStatus {
  DRAFT,
  SUBMITTED,
  APPROVED,
  REJECTED
}
